import React from "react";

const OurSponsorsBox = ({ title }) => {
    return (
        <div className="sponsor-box">
            <h2>{title}</h2>
        </div>
    );
};

export default OurSponsorsBox;
